# Python
__pycache__/
*.pyc
*.pyo

# 流水线2（已归档）的图片输出与运行文件
out_put/
download_progress.json
download_error_tasks.json
*.log

# 运行时锁与临时文件（os.replace 原子写的中转）
*.lock
*.tmp

# 大体积数据产物（一律不入库；spec_id.txt / *_test.csv 因上游历史仍被跟踪，不受 ignore 影响）
input/spec_id.txt
spec_id_pic_color_cnt_*.csv
error_tasks.json
master_error_tasks.json
